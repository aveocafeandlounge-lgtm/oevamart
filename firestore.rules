rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access for development
    // TODO: Restrict this to authenticated users in production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
